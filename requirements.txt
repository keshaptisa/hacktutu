# Минимальные версии вместо жёстких pin'ов: pip сам подберёт последний
# доступный wheel под вашу версию Python (важно для очень новых интерпретаторов,
# для которых бинарные колёса некоторых пакетов выходят с задержкой).
fastapi>=0.115.6
uvicorn[standard]>=0.34.0
httpx>=0.28.1
pydantic>=2.10.4
pydantic-settings>=2.7.0
jinja2>=3.1.5
python-dotenv>=1.0.1

# dev / tests
pytest>=8.3.4
pytest-asyncio>=0.25.0
